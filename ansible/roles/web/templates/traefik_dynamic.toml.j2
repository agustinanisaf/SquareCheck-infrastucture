[http.middlewares.simpleAuth.basicAuth]
  users = [
    "{{ traefik_dashboard_creds }}"
  ]

[http.routers.api]
  rule = "Host(`{{ monitor_domain }}`)"
  entrypoints = ["websecure"]
  middlewares = ["simpleAuth"]
  service = "api@internal"
  [http.routers.api.tls]
    certResolver = "lets-encrypt"
